/** ID of the main router-view element. */
export const ROUTER_VIEW_ID = "main";

/** ID of the Bootstrap navbar collapse element. */
export const NAVBAR_COLLAPSE_ID = "navbarCollapse";

/** Default scroll-based rotation speed (degrees per pixel scrolled). */
export const SCROLL_ROTATE_SPEED_DEFAULT = 0.15;

/** Hide fixed call button in last 10% of scroll range. */
export const HIDE_SCROLL_THRESHOLD = 0.9;
